{strip}
		{* This template is for a standard error message on pages that *shouldn't* be displaying if the user hasn't logged in.
		 In other words, it is an error to actually see this displayed in a browser. *}
	<div class="alert alert-danger">
		You must log in to view this information. Click <a href="/MyAccount/Login">here</a> to log in.
	</div>

{/strip}