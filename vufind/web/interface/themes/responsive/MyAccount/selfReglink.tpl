{strip}
	{if $enableSelfRegistration == 1 && $selfRegLink}
		<p id="selfRegLink" class="form-text">
			Don't have a library card? <a href="{$selfRegLink}">Register for a new Library Card</a>.
		</p>
	{/if}
{/strip}