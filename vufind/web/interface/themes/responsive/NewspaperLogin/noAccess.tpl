{strip}
	<div class="alert alert-warning">
		Sorry, {if $newspaperLibraryName}{$newspaperLibraryName}{*should display library name when the user home library is different than the interface's library*}{else}your library {/if} does not have access to this newspaper.
	</div>
{/strip}